(async()=>{try{let s='';for(let i=0;i<4;i++){const f=`gz.${String(i).padStart(2,'0')}.b64?v=0100`,r=await fetch(f,{cache:'no-store'});if(!r.ok)throw new Error(`${f} ${r.status}`);s+=await r.text()}const bin=atob(s.replace(/\s/g,'')),bytes=Uint8Array.from(bin,c=>c.charCodeAt(0));if(!('DecompressionStream'in window))throw new Error('browser needs gzip support');const html=await new Response(new Blob([bytes]).stream().pipeThrough(new DecompressionStream('gzip'))).text();document.open();document.write(html);document.close()}catch(e){document.body.innerHTML='<pre style="color:#efe7d3;background:#0e0d0d;padding:24px">LINEBURN failed to load: '+e.message+'</pre>'}})();
